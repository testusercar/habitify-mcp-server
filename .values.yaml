# Базовые настройки для MCP серверов

# Глобальные свойства
name: 'MCP Server' # Публичное название (e.g., "Habitify MCP Server") - для README, заголовков
slug: 'mcp-server' # Техническое имя (e.g., "habitify-mcp-server") - для репозитория, пакета, бинарных файлов
description: 'Base MCP server template' # Переопределяется в конкретных серверах
docsUrl: '' # Переопределяется в конкретных серверах
include: [] # OpenAPI operations to include. If empty, includes all operations
envVariables: [] # Environment variables configuration

# Настройки для package.json
package.json:
  version: '0.0.1'
  keywords: [] # Дополнительные keywords (базовые захардкожены в шаблоне)
  scripts: {} # Дополнительные scripts (базовые захардкожены в шаблоне)
  dependencies: {} # Дополнительные dependencies (базовые захардкожены в шаблоне)
  devDependencies: {} # Дополнительные devDependencies (базовые захардкожены в шаблоне)
  bin: {} # Bin configuration
  repository: '' # Git repository URL

# Настройки для README.md
README.md:
  features: [] # Переопределяется в конкретных серверах
